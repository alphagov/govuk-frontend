---
scenario: >-
  As part of an online service, you are asked to provide your phone number. Things to try:

  1. Do not answer the question before continuing to the next page.
---

{# This example is based of the  "What is your telephone number?" example: https://design-system.service.gov.uk/patterns/telephone-numbers/international/index.html #}
{% extends "full-page-example.njk" %}

{% from "back-link/macro.njk" import govukBackLink %}
{% from "button/macro.njk" import govukButton %}
{% from "error-summary/macro.njk" import govukErrorSummary %}
{% from "input/macro.njk" import govukInput %}

{% set pageTitle = "What is your telephone number?" %}
{% block pageTitle %}{{ "Error: " if errors }}{{ pageTitle }} - GOV.UK{% endblock %}

{% block beforeContent %}
  {{ govukBackLink({
    text: "Back",
    href: "#"
  }) }}
{% endblock %}

{% block content %}

  <form method="post" novalidate>
    {% if errorSummary.length > 0 %}
        {{ govukErrorSummary({
            titleText: "There is a problem",
            errorList: errorSummary
        }) }}
    {% endif %}

    {{ govukInput({
      label: {
        text: pageTitle,
        classes: "govuk-label--xl",
        isPageHeading: true
      },
      hint: {
        text: "For international numbers include the country code"
      },
      classes: "govuk-input--width-20 govuk-input--extra-letter-spacing",
      id: "telephone-number",
      name: "telephone-number",
      type: "tel",
      autocomplete: "tel",
      errorMessage: errors["telephone-number"]
    }) }}


    {{ govukButton({
        text: "Continue"
      }) }}
  </form>

{% endblock %}
