{% extends "template.njk" %}

{% from "checkboxes/macro.njk" import moalandCheckboxes %}

{% block head %}
  <!--[if !IE 8]><!-->
    <link rel="stylesheet" href="/public/app.css">
  <!--<![endif]-->
  <!--[if IE 8]>
    <link rel="stylesheet" href="/public/app-ie8.css">
  <![endif]-->
  <!--[if lt IE 9]>
    <script src="/vendor/html5-shiv/html5shiv.js"></script>
  <![endif]-->
{% endblock %}

{% block content %}
  <h1 class="moaland-heading-xl">Scoped initialisation</h1>
  <h2 class="moaland-heading-m">Unscoped section</h2>
  <p class="moaland-body">This example should intentionally not have JavaScript functionality</p>

  {{ moalandCheckboxes({
    idPrefix: "not-scoped",
    name: "not-scoped",
    items: [
      {
        value: "not-scoped",
        text: "Not scoped",
        conditional: {
          html: '<p class="moaland-body">Revealed not scoped text</p>'
        }
      }
    ]
  }) }}

  <h2 class="moaland-heading-m">Scoped section</h2>
  <p class="moaland-body">Only this example should have JavaScript functionality</p>
  <div id="scoped">
    {{ moalandCheckboxes({
      idPrefix: "scoped",
      name: "scoped",
      items: [
        {
          value: "scoped",
          text: "Scoped",
          conditional: {
            html: '<p class="moaland-body">Revealed scoped text</p>'
          }
        }
      ]
    }) }}
  </div>
{% endblock %}

{% block bodyEnd %}
  <script src="/public/all.js"></script>
  <script>
    var $scope = document.getElementById('scoped')
    window.MoalandFrontend.initAll({
      scope: $scope
    })
  </script>
{% endblock %}
