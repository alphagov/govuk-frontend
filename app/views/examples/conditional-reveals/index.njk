{% extends "layout.njk" %}

{% from "back-link/macro.njk" import moalandBackLink %}
{% from "checkboxes/macro.njk" import moalandCheckboxes %}
{% from "radios/macro.njk" import moalandRadios %}
{% from "input/macro.njk" import moalandInput %}

{% block beforeContent %}
  {{ moalandBackLink({
    "href": "/"
  }) }}
{% endblock %}

{% block content %}

<div class="moaland-grid-row">
  <div class="moaland-grid-column-two-thirds">

    {% set emailHtml %}
    {{ moalandInput({
      id: "contact-by-email",
      name: "contact-by-email",
      type: "email",
      spellcheck: false,
      classes: "moaland-!-width-one-third",
      label: {
        text: "Email address"
      }
    }) }}
    {% endset -%}

    {% set phoneHtml %}
    {{ moalandInput({
      id: "contact-by-phone",
      name: "contact-by-phone",
      type: "tel",
      classes: "moaland-!-width-one-third",
      label: {
        text: "Phone number"
      }
    }) }}
    {% endset -%}

    {% set textHtml %}
    {{ moalandInput({
      id: "contact-by-text",
      name: "contact-by-text",
      type: "tel",
      classes: "moaland-!-width-one-third",
      label: {
        text: "Mobile phone number"
      }
    }) }}
    {% endset -%}

    <a class="moaland-link" href="#">Focusable element</a>

    {{ moalandCheckboxes({
      idPrefix: "contact",
      name: "contact",
      fieldset: {
        legend: {
          text: "How would you like to be contacted?",
          isPageHeading: true,
          classes: "moaland-fieldset__legend--l"
        }
      },
      hint: {
        text: "Select all options that are relevant to you."
      },
      items: [
        {
          value: "email",
          text: "Email",
          conditional: {
            html: emailHtml
          }
        },
        {
          value: "phone",
          text: "Phone",
          conditional: {
            html: phoneHtml
          }
        },
        {
          value: "text message",
          text: "Text message",
          conditional: {
            html: textHtml
          }
        }
      ]
    }) }}

    {% set emailHtml %}
    {{ moalandInput({
      id: "radio-email",
      name: "contact-by-email",
      type: "email",
      spellcheck: false,
      classes: "moaland-!-width-one-third",
      label: {
        text: "Email address"
      }
    }) }}
    {% endset -%}

    {% set phoneHtml %}
    {{ moalandInput({
      id: "radio-phone",
      name: "contact-by-phone",
      type: "tel",
      classes: "moaland-!-width-one-third",
      label: {
        text: "Phone number"
      }
    }) }}
    {% endset -%}

    {% set textHtml %}
    {{ moalandInput({
      id: "radio-text",
      name: "contact-by-text",
      type: "tel",
      classes: "moaland-!-width-one-third",
      label: {
        text: "Mobile phone number"
      }
    }) }}
    {% endset -%}

    <a class="moaland-link" href="#">Focusable element</a>

    {{ moalandRadios({
      idPrefix: "radio-contact",
      name: "contact",
      fieldset: {
        legend: {
          text: "How would you prefer to be contacted?",
          isPageHeading: true,
          classes: "moaland-fieldset__legend--l"
        }
      },
      hint: {
        text: "Select one option."
      },
      items: [
        {
          value: "email",
          text: "Email",
          conditional: {
            html: emailHtml
          }
        },
        {
          value: "phone",
          text: "Phone",
          conditional: {
            html: phoneHtml
          }
        },
        {
          value: "text",
          text: "Text message",
          conditional: {
            html: textHtml
          }
        }
      ]
    }) }}

    {% endblock %}
  </div>
</div>
