---
scenario: >-
  As part of an online service, you are asked to upload your photo.

  The upload will always result in a success (alert) message that uses the notification banner.
---

{# This example is based of the live "Passport" service: https://www.passport.service.gov.uk/photo/upload #}
{% extends "_generic.njk" %}

{% from "back-link/macro.njk" import moalandBackLink %}
{% from "button/macro.njk" import moalandButton %}
{% from "checkboxes/macro.njk" import moalandCheckboxes %}
{% from "file-upload/macro.njk" import moalandFileUpload %}
{% from "notification-banner/macro.njk" import moalandNotificationBanner %}
{% from "phase-banner/macro.njk" import moalandPhaseBanner %}
{% from "radios/macro.njk" import moalandRadios %}

{% block pageTitle %}Upload your photo - GOV.MOA{% endblock %}

{% block header %}
  {% include "../../partials/banner.njk" %}
  {{ moalandHeader({
    serviceName: "Apply for a passport"
  }) }}
{% endblock %}

{% block beforeContent %}
    {{ moalandPhaseBanner({
        tag: {
            text: "beta"
        },
        html: 'This is a new service – your <a class="moaland-link" href="#">feedback</a> will help us to improve it.'
    }) }}
    {{ moalandBackLink({
        text: "Back",
        href: "#"
    }) }}
{% endblock %}

{% block content %}
  <div class="moaland-grid-row">
    <div class="moaland-grid-column-two-thirds">
        <form method="post" novalidate>
            {% if isSuccess %}
                {{ moalandNotificationBanner({
                "type": "success",
                "html": "<p class=\"moaland-notification-banner__heading\">Success, your photo has been uploaded</p>\n"
                }) }}
            {% endif %}

            <h1 class="moaland-heading-xl">Upload your photo</h1>

            <p class="moaland-body">
                Your photo will now go through an automated check.
            </p>

            <p class="moaland-body">
                You’ll need to review it before you add the photo to your application.
            </p>

            {{ moalandFileUpload({
                id: "photo",
                name: "photo",
                label: {
                    text: "Upload your photo"
                },
                hint: {
                    text: "Your photo must be at least 50KB and no more than 10MB"
                },
                errorMessage: errors["photo"]
            }) }}

            {{ moalandCheckboxes({
                idPrefix: "terms-and-conditions",
                name: "terms-and-conditions",
                items: [
                    {
                        value: "true",
                        html: 'I accept the <a class="moaland-link" href="#">terms and conditions</a>',
                        checked: values["terms-and-conditions"]
                    }
                ],
                errorMessage: errors["terms-and-conditions"]
            }) }}

            {{ moalandButton({
                text: "Submit your photo"
            }) }}
        </form>
    </div>
  </div>
{% endblock %}
