{% extends "layout.njk" %}

{% block content %}
  <h1 class="moaland-heading-xl">
    GOV.MOA Frontend
  </h1>

  <div class="moaland-grid-row">

    <div class="moaland-grid-column-one-third">
      <h2 class="moaland-heading-m">Components</h2>

      <ul class="moaland-list">
        <li><a href="/components/all{{ legacyQuery }}" class="moaland-link">All</a></li>
        {% for componentName in componentsDirectory | sort %}
          <li><a href="/components/{{ componentName }}{{ legacyQuery }}" class="moaland-link">{{ componentName | replace("-", " ") | capitalize }}</a></li>
        {% endfor %}
      </ul>
    </div>

    <div class="moaland-grid-column-one-third">
      <h2 class="moaland-heading-m">Examples</h2>

      <ul class="moaland-list">
      {% for exampleName in examplesDirectory | sort %}
        <li><a href="/examples/{{ exampleName }}{{ legacyQuery }}" class="moaland-link">{{ exampleName | replace("-", " ") | capitalize }}</a></li>
      {% endfor %}
      </ul>
    </div>

    <div class="moaland-grid-column-one-third">
      <h2 class="moaland-heading-m"><a href="/full-page-examples" class="moaland-link">Full page examples</a></h2>

      <ul class="moaland-list">
      {% for example in fullPageExamples %}
        <li><a href="/full-page-examples/{{ example.path }}{{ legacyQuery }}" class="moaland-link">{{ example.name | replace("-", " ") | capitalize }}</a></li>
      {% endfor %}
      </ul>

      <h2 class="moaland-heading-m">Misc</h2>

      <ul class="moaland-list">
        <li><a href="/docs" class="moaland-link">Sass Documentation</a></li>
      </ul>
    </div>
  </div>
{% endblock %}
