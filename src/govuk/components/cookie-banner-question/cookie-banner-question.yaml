params:
  - name: title
    type: string
    required: true
    description: Title for the cookie banner question
  - name: text
    type: string
    required: false
    description: Descriptive text for the cookie banner question. Ignored if html is provided
  - name: html
    type: string
    required: false
    description: Descriptive text for the cookie banner question. Text is ignored if html is provided
  - name: acceptButtonText
    type: string
    required: true
    description: Text for the accept button
  - name: rejectButtonText
    type: string
    required: true
    description: Text for the reject button
  - name: link
    type: object
    required: true
    description: Used to link to cookie settings page
    params:
      - name: text
        type: string
        required: true
        description: Link text
      - name: href
        type: string
        required: true
        description: Link href

examples:
- name: default
  data:
    title: Cookies on [service name]
    html: <p>We use cookies to make this service work</p><p>We'd like to set some additional cookies so we can remember your settings, understand how you use the service and make improvements.</p>
    acceptButtonText: Accept cookies
    rejectButtonText: Reject cookies
    link:
      text: View cookies and set preferences
      href: /cookie-preferences
