{% from "../button/macro.njk" import govukButton %}
{% set redirectUrl = params.redirectUrl | default("https://www.google.com") -%}

<div
  {%- if params.id %} id="{{ params.id }}"{% endif %}
  class="govuk-exit-this-page {%- if params.classes %} {{ params.classes }}{% endif %}"
  data-module="govuk-exit-this-page" {%- for attribute, value in params.attributes %} {{attribute}}="{{value}}"{% endfor %}
>
  {{ govukButton({
    text: params.text,
    classes: "govuk-button--warning govuk-exit-this-page__button govuk-js-exit-this-page-button",
    href: redirectUrl
  })}}
</div>
