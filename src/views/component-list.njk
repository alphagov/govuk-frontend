{% extends "layout.njk" %}

{% block content %}
  <h1 class="govuk-u-heading-36">
    GOV.UK Frontend
  </h1>
  <!-- Show the list of components -->
  <ul class="govuk-c-list">
  {% for children in componentsDirectory.children %}
    <li><a href="/components/{{ children.name }}">{{ children.name }}</a></li>
  {% endfor %}
  </ul>
{% endblock %}
